<form>
  <div class="form-group">
    <label>Acquis à greffer</label>
    <select name="item" value="{{item}}">
      {{#each itemsNotGraft as |item|}}
        <option value="{{item.id}}">{{item.name}}</option>
      {{/each}}
    </select>
  </div>
</form>